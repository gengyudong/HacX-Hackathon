import { Box, Typography } from "@mui/material";

export default function PhotoAnalysis() {
    return (
        <Box>
        <Typography variant="h2">Photo Analysis Page</Typography>
        </Box>
    );
}