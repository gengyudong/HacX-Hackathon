import { Box, Typography } from "@mui/material";

export default function PhotoLanding() {
    return (
        <Box>
        <Typography variant="h5">Photo Landing Page</Typography>
        </Box>
    );
}
